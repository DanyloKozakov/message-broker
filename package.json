{
  "name": "two-worker-message-broker",
  "version": "1.0.0",
  "private": true,
  "description": "A small HTTP service that coordinates two workers in repeating rounds.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
